const table = document.querySelector(".table");
const list = table.querySelector(".list");
const prev = table.querySelector(".pagination__button--next");
const next = table.querySelector(".pagination__button--prev");

console.log(next);
